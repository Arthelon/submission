extends ./layout

block content
    .header
        .btns
            a(href='/') Main
    .container
        h1= message
        if !{error}
            h2= error.status
            pre #{error.stack}
